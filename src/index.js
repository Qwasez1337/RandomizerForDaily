import React from 'react';
import ReactDOM from 'react-dom/client';
import FairnessCycleTest from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <FairnessCycleTest />
  </React.StrictMode>
);